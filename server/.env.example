# Copy to server/.env for local dev. On Railway, set these as service Variables instead.
DATABASE_URL=postgresql://user:password@localhost:5432/shrp_shopfloor
JWT_SECRET=change-this-to-a-long-random-string
PORT=8080
